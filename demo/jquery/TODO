RUN WITH:

  /usr/local/bin/python c2html.py stringlist.c [stringlist.c.json]


. Single binary (csolve foo.c) generates the HTML
   >> Binary calls c2html.py
   >> Copies the css, js from the relevant directories

HEREHEREHEREHERE

. Render "Concrete" predicates too.

. Add Anchor To each line number

. Cleanup Variable names
        >> Link to line.

. Print FUNCTION types

. Add error slicing.
   Error: Root + lines in slice
   Slice: Hide all lines EXCEPT slice
