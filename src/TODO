- nuke the ghastly .bnd state threaded through theoremProverZ3.ml
- make the constraints "polymorphic" (so we can tag with src-locations *)

**************************************************************

[pr] inline all functions (drop-recursive calls)
[pr] add globals as locals after inlining

[rj] add "array-bounds-assertion"
[rj] combine: types + refanno + consgen

**************************************************************
The great merge:

4. Implement new signatures of consinfra, fixInterface
5. UPDATE: Consinfra.ctype_of_varinfo: Consinfra.t -> Cil.varinfo -> Ctypes.ctype
   so that it uses the theta -- map and returns the concrete location...

6. implement consgen using notes and new interfaces (4,5)
	-- halfway: have threaded "world" through the consgen functions
	-- next: generate constraints in:
		cons_of_set
		cons_of_annot

--^^^-- get small benchmarks working on above --^^^---

- add functions

**************************************************************
- globals
- functions
**************************************************************
------------------------
while e do:
  s1
s2

---------equals---------

while (1) do: 
  if e: 
    [] 
  else: 
    goto L:;
  s1;
L: s2

---------equals---------

while (1) do:
  if (not e):
    goto L:
  else:
    []
  s1

L: s2

------------------------


