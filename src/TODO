- nuke the ghastly .bnd state threaded through theoremProverZ3.ml
- make the constraints "polymorphic" (so we can tag with src-locations *)

**************************************************************

[pr] inline all functions (drop-recursive calls)
[pr] add globals as locals after inlining

[rj] add "array-bounds-assertion"
[rj] combine: types + refanno + consgen

**************************************************************
The great merge:
- refactor [tcons_of_phis] to use [make_cs_block] instead of low-level loop.

Implement new functions (store/ldesc related) of fixInterface.mli

--^^^-- get small benchmarks working on above --^^^---

- add functions

foldn: (int -> 'a -> 'b prectype -> 'a) -> 'a -> 'b LDesc.t -> 'a

**************************************************************
- globals
- functions
**************************************************************
------------------------
while e do:
  s1
s2

---------equals---------

while (1) do: 
  if e: 
    [] 
  else: 
    goto L:;
  s1;
L: s2

---------equals---------

while (1) do:
  if (not e):
    goto L:
  else:
    []
  s1

L: s2

------------------------


